'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/Button';import { Input } from '@/components/ui/Input';
type Category={id:string;name:string;slug:string;description:string};
const empty={name:'',slug:'',description:''};
export function CategoriesManager({initial}:{initial:Category[]}){const router=useRouter();const [form,setForm]=useState(empty);const [editing,setEditing]=useState<string|null>(null);const [error,setError]=useState('');const save=async(e:React.FormEvent)=>{e.preventDefault();const response=await fetch(editing?`/api/admin/categories/${editing}`:'/api/admin/categories',{method:editing?'PUT':'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});if(!response.ok){setError((await response.json()).error);return;}setForm(empty);setEditing(null);setError('');router.refresh();};return <><form onSubmit={save} className="mt-6 grid gap-3 rounded-2xl border bg-card p-5 md:grid-cols-[1fr_1fr_2fr_auto]"><Input placeholder="Name" required value={form.name} onChange={e=>setForm({...form,name:e.target.value,slug:editing?form.slug:e.target.value.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')})}/><Input placeholder="Slug" required value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})}/><Input placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><Button>{editing?'Update':'Add'}</Button>{error&&<p className="text-sm text-destructive md:col-span-4">{error}</p>}</form><div className="mt-6 divide-y rounded-2xl border bg-card">{initial.map(c=><div key={c.id} className="flex flex-col gap-3 p-4 md:flex-row md:items-center"><div className="flex-1"><p className="font-medium">{c.name} <span className="text-sm text-muted-foreground">/{c.slug}</span></p><p className="text-sm text-muted-foreground">{c.description}</p></div><Button variant="outline" onClick={()=>{setEditing(c.id);setForm({name:c.name,slug:c.slug,description:c.description});}}>Edit</Button><Button variant="ghost" onClick={async()=>{if(confirm('Delete this category?')){const response=await fetch(`/api/admin/categories/${c.id}`,{method:'DELETE'});if(!response.ok)setError((await response.json()).error);else router.refresh();}}}>Delete</Button></div>)}</div></>}
