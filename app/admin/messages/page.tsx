import { desc } from 'drizzle-orm';import { db } from '@/lib/db';import { messages } from '@/lib/db/schema';import { MessagesManager } from './MessagesManager';export const dynamic='force-dynamic';export default async function MessagesPage(){return <><h1 className="font-serif text-3xl font-semibold">Messages</h1><MessagesManager initial={await db.select().from(messages).orderBy(desc(messages.createdAt))}/></>}
